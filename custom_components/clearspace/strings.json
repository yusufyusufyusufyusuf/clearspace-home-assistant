{
  "config": {
    "step": {
      "user": {
        "title": "Connect ClearSpace",
        "description": "Create a ClearSpace API token in settings, then connect one account per token. You can add more later.",
        "data": {
          "name": "Account name",
          "api_token": "API token",
          "api_url": "API URL",
          "entity_prefix": "Entity prefix",
          "refresh_interval": "Refresh interval (seconds)",
          "is_default": "Use as default account for services"
        }
      }
    },
    "error": {
      "cannot_connect": "Could not connect to ClearSpace",
      "invalid_auth": "The API token is invalid or revoked"
    },
    "abort": {
      "already_configured": "This API token is already configured"
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "ClearSpace account settings",
        "description": "Change the entity prefix, refresh interval, or which account is the default target for services.",
        "data": {
          "entity_prefix": "Entity prefix",
          "refresh_interval": "Refresh interval (seconds)",
          "is_default": "Use as default account for services"
        }
      }
    }
  }
}
